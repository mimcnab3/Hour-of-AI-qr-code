# hoai-qrcode
coding Agent example for qr code generation
this website will generate a qr code from a url or one line of text
there is a text box and the qr code is generated below and a button is shown below the qr code where it lets you download the qr code as a png image this code is generated by a AI agent
as a example of how to use it

TESTING
=========

serve this folder with a simple http server and open the page for testing
this website is using the default github pages workflow
